# AGENTS.md

## 專案簡介

本專案為「睡眠健康 App」介面設計迭代優化。目標是透過持續生成同評分,
產出一個既符合視覺美感、又真正適合深夜疲累用戶操作嘅介面設計。

核心差異化: 呢個App解決嘅係「失眠成因」(腦內雜念卸載、認知重構),
而唔係單純追蹤睡眠數據或者提供白噪音/冥想內容。

## 評分文件(Source of Truth)

所有版本評分、迭代決策,必須以本資料夾內以下三份文件為依據,
不可自行假設或發明標準:

- `rubric_version_*.md` — 評分標準,包含強制否決規則、評分維度、權重、
  Persona Simulation流程、迭代多樣性檢查機制
- `case_version_*.md` — 已校準嘅 few-shot 判決案例庫(pass/fail/borderline)
- `judge_prompt_version_*.md` — 可直接接續 `/goal` 使用嘅完整judge指令範本

**版本規則**: 檔名數字愈大代表愈新。執行任何評分或 `/goal` 任務前,
必須先確認資料夾內三份文件嘅最新版本號,並以最新版本為準。
如果三份文件版本號不一致(例如 rubric 已到 v3 但 case 仍係 v2),
先向用戶提出確認,唔好自行假設應該用邊個組合。

## 核心工作流程

每一輪產出新版本後,必須依序執行:

1. 用 Playwright 對6個核心頁面截圖(Onboarding/首頁/記錄頁/睡眠頁/
   Wind-Down頁/我的)
2. 先執行迭代多樣性檢查(見 rubric 第5節)— 比對新版本與上一輪版本
   喺色彩家族、佈局結構、元件造型三層面是否有可辨識改變
3. 檢查全部強制否決規則(rubric 第1節),任何一項不合格即判0分,
   不進入加權評分
4. 執行 Persona Simulation(扮演凌晨1點仲瞓唔著、好攰嘅香港上班族),
   用模擬結果評行為/可用性維度
5. 逐維度評分,並引用 case 文件入面對應嘅 Case ID 作為判斷依據
6. 計算加權總分(行為/可用性50% + 情緒/功能對齊35% + 純美觀/原創性15%)

## 硬性規則(不可協商)

- 全局框架:頂部欄 + 底部導覽4分頁 + FAB,不可增減
- 6個核心頁面內容範圍不可刪減或合併
- 唔可以用高飽和刺激色、密集資訊、急促動畫
- 輔助色不可選用帶「警覺/活力」聯想嘅色相(黃、亮橙、亮紅)作為
  核心互動元件嘅主要輔助色
- 全App icon必須統一風格家族,不可在同一版本內混用outline同filled
  等不同繪製邏輯
- 每一輪新版本必須喺色彩家族、佈局結構、元件造型其中至少一個層面,
  做出肉眼可辨識嘅方向性改變,不可只做參數微調充當新版本

## 案例庫維護規則

每完成一輪 generation 之後:

- 若發現新嘅 PASS/FAIL/BORDERLINE 案例,追加入最新版本嘅
  `case_version_*.md`,並使用固定欄位格式:版本、頁面、維度、判決、
  嚴重度、證據、原因、可遷移規則
- 若某條規則需要修改(例如判斷門檻調整、新增維度),升級
  `rubric_version_*.md` 到下一個版本號,不可直接覆蓋舊版本
- 唔好將完整案例庫內容貼入本 AGENTS.md,案例庫應保持獨立檔案,
  持續增長

## 溝通語言

與用戶溝通一律使用廣東話(口語書面化),技術術語可保留英文原文。
